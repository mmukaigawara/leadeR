% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta.R
\name{get_lta}
\alias{get_lta}
\title{Leadership Trait Analysis (LTA)}
\usage{
get_lta(own_entity, text, bootstrap = FALSE, B = 1000)
}
\arguments{
\item{own_entity}{A character string identifying the speaker's country or
entity (e.g., \code{"United States"}).}

\item{text}{A character string containing the speech text to analyse.}

\item{bootstrap}{Logical. If \code{TRUE}, returns bootstrap means and variances
instead of raw counts. Default is \code{FALSE}.}

\item{B}{Number of bootstrap iterations. Default is \code{1000}.}
}
\value{
A one-row \link[tibble:tibble]{tibble::tibble}.

When \code{bootstrap = FALSE}, columns include raw counts (\code{P}, \code{OP}, \code{A},
\code{OA}, \code{S}, \code{OS}, \code{HC}, \code{LC}, \code{TI}, \code{IP}, \code{SC}, \code{OSC}, \code{N}, \code{ON}, \code{IC},
\code{OC}) plus trait proportions (\code{Pp}, \code{D}, \code{C}, \code{Ta}, \code{Ss}, \code{Na}, \code{B}).

When \code{bootstrap = TRUE}, columns include bootstrap means and variances
for the raw counts (\code{meanP}, \code{varP}, \code{meanOP}, \code{varOP}, ...) plus trait
proportions and their delta-method variances (\code{Pp}, \code{varPp}, \code{D}, \code{varD},
\code{C}, \code{varC}, \code{Ta}, \code{varTa}, \code{Ss}, \code{varSs}, \code{Na}, \code{varNa}, \code{B}, \code{varB}).
}
\description{
Runs all eight LTA trait functions at once and returns a single combined
tibble. The eight traits are: power, affiliation, distrust, conceptual
complexity, task orientation, self-confidence, nationalism, and control.
}
\examples{
\donttest{
# Requires spaCy to be installed; see spacyr::spacy_install().
spacyr::spacy_initialize()
get_lta(own_entity = "United States", text = "We will defend our nation.")
get_lta(own_entity = "United States", text = "We will defend our nation.",
        bootstrap = TRUE, B = 500)
}
}
